'use client';
import { useEffect, useState } from 'react';
import { BarChart3, Loader2 } from 'lucide-react';
export default function StudentAttendancePage(){const [data,setData]=useState<any>(null); useEffect(()=>{fetch('/api/student/summary').then(r=>r.json()).then(setData)},[]); if(!data)return <div className="flex min-h-[400px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>; const s=data.summary; return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><BarChart3 size={26}/>Attendance Percentage</h1><p className="mt-1 text-sm text-slate-500">Overall attendance and non-collegiate risk level.</p></div><div className="grid grid-cols-1 gap-4 md:grid-cols-4"><div className="card p-5"><p className="text-sm font-bold text-slate-500">Attendance</p><div className="mt-2 text-4xl font-black text-blue-700">{s?.percentage||0}%</div></div><div className="card p-5"><p className="text-sm font-bold text-slate-500">Risk</p><div className="mt-2 text-4xl font-black text-slate-900">{s?.riskLevel||'N/A'}</div></div><div className="card p-5"><p className="text-sm font-bold text-slate-500">Attended</p><div className="mt-2 text-4xl font-black text-emerald-700">{s?.attended||0}</div></div><div className="card p-5"><p className="text-sm font-bold text-slate-500">Absences</p><div className="mt-2 text-4xl font-black text-red-700">{s?.absences||0}</div></div></div></div>}
