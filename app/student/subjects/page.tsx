'use client';
import { useEffect, useState } from 'react';
import { GraduationCap, Loader2 } from 'lucide-react';
export default function StudentSubjectsPage(){const [data,setData]=useState<any>(null); useEffect(()=>{fetch('/api/student/summary').then(r=>r.json()).then(setData)},[]); return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><GraduationCap size={26}/>Subject-wise Attendance</h1><p className="mt-1 text-sm text-slate-500">Subject-wise attendance percentage and risk status.</p></div>{!data?<div className="flex min-h-[300px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>:<div className="card overflow-hidden"><table><thead><tr><th>Subject</th><th>Attended</th><th>Total</th><th>Percentage</th><th>Risk</th></tr></thead><tbody>{(data.subjects||[]).map((s:any)=><tr key={s.subject}><td className="font-bold">{s.subject}</td><td>{s.attended}</td><td>{s.total}</td><td>{s.percentage}%</td><td>{s.riskLevel}</td></tr>)}</tbody></table></div>}</div>}
