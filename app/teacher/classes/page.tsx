'use client';
import { useEffect, useState } from 'react';
import { GraduationCap, Loader2 } from 'lucide-react';
export default function TeacherClassesPage(){const [data,setData]=useState<any>(null); useEffect(()=>{fetch('/api/teacher/summary').then(r=>r.json()).then(setData)},[]); const classes=Array.from(new Set((data?.logs||[]).map((l:any)=>`${l.class}-${l.section}`))); return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><GraduationCap size={26}/>My Classes</h1><p className="mt-1 text-sm text-slate-500">Classes and sections where you have taken attendance.</p></div>{!data?<div className="flex min-h-[300px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>:<div className="grid grid-cols-1 gap-4 md:grid-cols-3">{classes.length?classes.map((c:any)=><div key={c} className="card p-5"><div className="text-sm font-bold text-slate-500">Class</div><div className="mt-2 text-3xl font-black text-blue-700">{c}</div></div>):<div className="card p-10 text-center text-slate-500 md:col-span-3">No classes found yet. Scan QR to create class activity.</div>}</div>}</div>}
