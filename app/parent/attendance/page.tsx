'use client';
import { useEffect, useState } from 'react';
import { BarChart3, Loader2 } from 'lucide-react';
export default function ParentAttendancePage(){const [data,setData]=useState<any>(null); useEffect(()=>{fetch('/api/parent/children').then(r=>r.json()).then(setData)},[]); return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><BarChart3 size={26}/>Child Attendance</h1><p className="mt-1 text-sm text-slate-500">View child attendance percentage and recent records.</p></div>{!data?<div className="flex min-h-[300px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>:<div className="space-y-5">{(data.children||[]).map((item:any)=><div key={item.student._id} className="card overflow-hidden"><div className="border-b bg-slate-50 px-5 py-4"><h2 className="font-black">{item.student.name}</h2><p className="text-sm text-slate-500">Class {item.student.class}-{item.student.section} • Roll {item.student.rollNumber}</p></div><div className="grid grid-cols-1 gap-4 p-5 md:grid-cols-4"><div><p className="text-sm font-bold text-slate-500">Attendance</p><p className="text-3xl font-black text-blue-700">{item.summary.percentage}%</p></div><div><p className="text-sm font-bold text-slate-500">Risk</p><p className="text-3xl font-black">{item.summary.riskLevel}</p></div><div><p className="text-sm font-bold text-slate-500">Attended</p><p className="text-3xl font-black text-emerald-700">{item.summary.attended}</p></div><div><p className="text-sm font-bold text-slate-500">Absences</p><p className="text-3xl font-black text-red-700">{item.summary.absences}</p></div></div></div>)}</div>}</div>}
