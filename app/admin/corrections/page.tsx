'use client';
import { useEffect, useState } from 'react';
import { Loader2, RefreshCw } from 'lucide-react';
type Correction = { _id:string; attendanceId:string; previousStatus:string; requestedStatus:string; reason:string; status:string; createdAt:string };
export default function CorrectionsPage(){const [items,setItems]=useState<Correction[]>([]); const [loading,setLoading]=useState(true); async function load(){setLoading(true); const res=await fetch('/api/admin/corrections'); const data=await res.json(); setItems(data.items||[]); setLoading(false)} useEffect(()=>{load()},[]); return <div><div className="mb-6 flex justify-between gap-4"><div><h1 className="m-0 text-2xl font-black text-slate-900">Attendance Corrections</h1><p className="mt-1 text-sm text-slate-500">Review attendance correction requests and audit trail.</p></div><button onClick={load} className="inline-flex items-center gap-2 rounded-xl border border-blue-200 bg-blue-50 px-4 py-2 text-sm font-bold text-blue-700"><RefreshCw size={16}/>Refresh</button></div><div className="card overflow-hidden">{loading?<div className="p-10"><Loader2 className="animate-spin"/></div>:items.length===0?<div className="p-10 text-center text-slate-500">No correction requests found.</div>:<table><thead><tr><th>Attendance</th><th>Previous</th><th>Requested</th><th>Reason</th><th>Status</th><th>Date</th></tr></thead><tbody>{items.map(i=><tr key={i._id}><td>{i.attendanceId}</td><td>{i.previousStatus}</td><td>{i.requestedStatus}</td><td>{i.reason}</td><td><span className="badge badge-warning">{i.status}</span></td><td>{new Date(i.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table>}</div></div>}
