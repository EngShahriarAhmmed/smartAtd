'use client';
import { useEffect, useState } from 'react';
import { Loader2, Settings } from 'lucide-react';
export default function AdminSettingsPage(){const [data,setData]=useState<any>(null); useEffect(()=>{fetch('/api/admin/settings').then(r=>r.json()).then(setData)},[]); return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><Settings size={26}/>Institution Settings</h1><p className="mt-1 text-sm text-slate-500">Settings are available only for Super Admin and Institution Admin/Admin.</p></div>{!data?<div className="flex min-h-[300px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>:<div className="grid grid-cols-1 gap-4 lg:grid-cols-2"><div className="card p-6"><h2 className="text-lg font-black">Institution Profile</h2><div className="mt-4 space-y-3 text-sm"><p><b>Name:</b> {data.institution?.name || 'Platform Workspace'}</p><p><b>Code:</b> {data.institution?.code || 'N/A'}</p><p><b>Address:</b> {data.institution?.address || 'N/A'}</p><p><b>Status:</b> {data.institution?.status || 'Active'}</p></div></div><div className="card p-6"><h2 className="text-lg font-black">Security Defaults</h2><div className="mt-4 space-y-3 text-sm text-slate-600"><p>• QR payload contains encrypted token only.</p><p>• Attendance duplicate prevention is enabled per period.</p><p>• Settings menu is restricted to Super Admin/Admin roles.</p><p>• Student, Parent and Teacher portals cannot access settings.</p></div></div></div>}</div>}
