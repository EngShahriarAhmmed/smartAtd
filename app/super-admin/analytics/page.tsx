'use client';
import { useEffect, useState } from 'react';
import { BarChart3, Building2, GraduationCap, Loader2, ScanLine, Users } from 'lucide-react';
function Card({label,value,icon}:{label:string;value:number;icon:React.ReactNode}){return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-3 flex items-center justify-between text-blue-700"><span className="text-sm font-bold text-slate-500">{label}</span>{icon}</div><div className="text-3xl font-black text-slate-900">{value}</div></div>}
export default function AnalyticsPage(){const [data,setData]=useState<Record<string,number>|null>(null); useEffect(()=>{fetch('/api/super-admin/overview').then(r=>r.json()).then(setData)},[]); if(!data)return <div className="flex min-h-[400px] items-center justify-center"><Loader2 className="animate-spin" size={44}/></div>; return <div><div className="mb-6"><h1 className="m-0 flex items-center gap-2 text-2xl font-black text-slate-900"><BarChart3 size={26}/>Usage Analytics</h1><p className="mt-1 text-sm text-slate-500">SaaS-level institution, user, attendance and notification analytics.</p></div><div className="grid grid-cols-1 gap-4 md:grid-cols-3 xl:grid-cols-6"><Card label="Institutions" value={data.institutions||0} icon={<Building2/>}/><Card label="Users" value={data.users||0} icon={<Users/>}/><Card label="Students" value={data.students||0} icon={<GraduationCap/>}/><Card label="Teachers" value={data.teachers||0} icon={<Users/>}/><Card label="Attendance" value={data.attendance||0} icon={<ScanLine/>}/><Card label="Pending Alerts" value={data.pendingNotifications||0} icon={<BarChart3/>}/></div></div>}
